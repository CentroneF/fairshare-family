# UX changes 

1. In this image of the app: decline-button-issue.png, the decline button is changing size when an error is shown.
2. In the expense list, each expense doesn't show who created it. This might require a bigger change: the user should have a name. We can add it to the menu under the email address. 
3. The Confirm settlement button should not be visible at all if its the current month.
4. In the declined expense item in the expense list, the text of the reason is not clear that is the decline reason: add a label
5. in the desktop view of the menu, the buttons should occupy the full width of the menu
